custom java agent to track exactly which instruction (at which offset) caused an error.

Usage:
   # compile agent:
   make

   #run with agent
   java -agentpath:/path/to/compiled_agent <MainClass>
   
   # test npe with addition info
   make test_npe
